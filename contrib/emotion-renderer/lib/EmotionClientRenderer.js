"use strict";

const ReactClientRenderer = require("@gourmet/react-renderer/lib/ReactClientRenderer");

// Currently, there is nothing to do for emotion on client-side
module.exports = ReactClientRenderer;
