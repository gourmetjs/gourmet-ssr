{
  "name": "@gourmet/test-builder-basic",
  "version": "0.7.2",
  "private": true,
  "scripts": {
    "build": "gourmet build --config-name gourmet_cfg",
    "start": "node lib/server.js",
    "test": "npm run test:local && npm run test:prod && npm run test:ltc",
    "test:local": "gourmet build --config-name gourmet_cfg --verbose 1 && tape test/*_test.js",
    "test:prod": "gourmet build --config-name gourmet_cfg --verbose 1 --stage prod && tape test/*_test.js --stage prod",
    "test:ltc": "gourmet build --config-name gourmet_cfg --verbose 1 --stage ltc && tape test/*_test.js --stage ltc"
  },
  "dependencies": {
    "@gourmet/client-lib": "^0.6.2",
    "@gourmet/server-args": "^0.4.0",
    "express": "^4.16.3",
    "morgan": "^1.9.0"
  },
  "devDependencies": {
    "@gourmet/gourmet-cli": "^0.5.0",
    "@gourmet/plugin-webpack-babel": "^0.6.0",
    "@gourmet/plugin-webpack-builder": "^0.7.2",
    "@gourmet/promise-tape": "^0.3.0",
    "@gourmet/puppeteer-args": "^0.4.0",
    "puppeteer": "^1.3.0",
    "tape": "^4.9.0"
  }
}
