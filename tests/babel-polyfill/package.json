{
  "name": "@gourmet/test-babel-polyfill",
  "version": "1.5.0",
  "private": true,
  "scripts": {
    "build": "gourmet build",
    "test": "gourmet build --verbosity 1 && gourmet --stage entry build --verbosity 1 && tape test/*_test.js"
  },
  "devDependencies": {
    "@gourmet/gourmet-cli": "^1.1.4",
    "@gourmet/preset-core": "^1.4.0",
    "core-js": "^3.0.0",
    "tape": "^4.9.0"
  }
}
