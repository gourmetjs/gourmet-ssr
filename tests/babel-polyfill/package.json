{
  "name": "@gourmet/test-babel-polyfill",
  "version": "1.2.9",
  "private": true,
  "scripts": {
    "build": "gourmet build",
    "test": "gourmet build --verbosity 1 && gourmet build --verbosity 1 --stage entry && tape test/*_test.js --verbosity 1"
  },
  "devDependencies": {
    "@gourmet/gourmet-cli": "^1.1.4",
    "@gourmet/preset-core": "^1.1.9",
    "tape": "^4.9.0"
  }
}
